<?xml version="1.0" encoding="utf-8"?>
<!--
    DEBUG BUILDS ONLY. This file lives in src/debug, so it is not in a release APK.

    A development server speaks http, and Android has blocked cleartext by default
    since Android 9. Rather than opening cleartext for everything, this permits it for
    exactly two hosts:

      10.0.2.2   the host machine as seen from the Android emulator
      localhost  adb reverse tcp:8000 tcp:8000

    Release builds keep the default: https only.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
