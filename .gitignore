# Local secrets
.env
.env.*
!.env.example

# Operating system files
.DS_Store
Thumbs.db

# Tooling scratch output (브라우저 검증 / web fetch / pdf rendering)
output/
pdfs/
pdfout/

# 격리 작업용 git worktree. 저장소 안에 생기지만 자체 체크아웃이라 추적 대상이 아니다.
.claude/worktrees/

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# 발표 자료 빌드 중간물
pitch/.build/

# Playwright MCP/CLI 산출물 (스냅샷·콘솔 로그)
.playwright-mcp/
.playwright-cli/
