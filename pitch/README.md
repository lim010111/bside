# 발표 자료

2026-09-20 BLE 전환에 맞춰 덱을 고쳐 썼다. [script.md](script.md)의 흐름을 10장으로 옮긴 것이다.

**5장(시연)은 비워 두었다.** 무엇을 보여줄지는 촬영 직전에 정한다. 실기기 두 대에서 BLE 발견이
되면 그것을 찍고, 안 되면 지금 동작하는 상호작용 층을 찍고 발견은 다음 단계라고 말한다.
덱의 나머지 아홉 장은 어느 쪽이든 그대로 쓴다.

| 파일 | 용도 |
|---|---|
| [deck.html](deck.html) | **원본.** 이것만 고치면 된다 |
| [assets/](assets/) | 덱에 박힌 제품 화면. `web/`을 띄워 직접 찍은 것. [다시 찍기](#화면-캡처-다시-찍기) |
| [check.py](check.py) | 구조 검사. 빌드가 먼저 돌린다 |
| `Bside_발표자료.pdf` / `.pptx` | **제출·발표용 본편 10장** |
| `Bside_백업.pdf` / `.pptx` | **질의응답용 5장.** 발표에는 안 넘긴다 |
| [talk.md](talk.md) | **5분 발표 대본.** 덱 10장과 시간이 맞춰져 있다 |
| [script.md](script.md) | BLE MVP 발표 흐름. 덱이 이것을 따른다 |

## 발표할 때

`deck.html`을 크롬으로 열고 `F11`. **방향키·스페이스**로 넘기고, 화면 왼쪽 1/4을 클릭하면 뒤로 간다.

영상과 폰트가 네트워크를 타므로 **행사장에서는 PPTX로 발표하는 게 안전하다.** PPTX는 각 장이 이미지라 폰트가 깨질 일이 없다.

## 구성

본편 10장. 시간 배분은 [talk.md](talk.md)와 맞춰져 있다.

| 장 | 내용 | 시간 | 배점 | BLE 전환 |
|---|---|---|---|---|
| 1 | 표지 | 0:00–0:15 | | 한 줄 소개에서 행사 종속 제거 |
| 2 | 문제 — 가정한 30분 | 0:15–0:55 | 아이디어 30 | 그대로 |
| 3 | 기존 행사 앱의 한계 | 0:55–1:25 | 아이디어 30 | 그대로 |
| 4 | 서비스 흐름 | 1:25–1:40 | | **발견 단계로 다시 씀** |
| 5 | **시연 영상 80초** | 1:40–3:00 | MVP 25 | **비어 있음** |
| 6 | 상보성 대조 | 3:00–3:40 | 아이디어 30 · AI 15 | 그대로. 두 방향 모두에서 참 |
| 7 | AI 경계선과 구조 | 3:40–4:05 | AI 15 | 근접 관측 유효성으로 바꿈 |
| 8 | MVP와 로드맵 | 4:05–4:25 | MVP 25 | **BLE·알림을 로드맵으로** |
| 9 | 누가 돈을 내는가 | 4:25–4:50 | 사업화 15 | **상시 공간 추가, 밀도는 미검증으로** |
| 10 | 마무리 | 4:50–5:00 | 발표 15 | 그대로 |

문제 구간이 80초로 전체의 27%다. 반복 우승 팀이 문제 맥락에 약 30%를 쓴다는 조사에 맞췄다.

### 바꾸지 않은 것

2·3·6장은 손대지 않았다. 문제와 상보성은 발견 방식과 무관하게 성립한다.
**6장이 이 발표의 최고점이고, 그것이 BLE 전환에도 살아남는다는 점이 이 덱의 뼈대다.**

### 백업 장에서 고친 것

- `같은 방 전체 목록` → `주변 참여자 전체 목록`
- `참여 중단하면 새 메시지가 막힙니다` → **틀렸다.** 발견 OFF는 새 대화 시작만 막고
  이미 시작한 대화는 송수신이 유지된다
- `행사 종료 1시간 뒤 삭제` → **폐기됐다.** B14는 시간 경과만으로 하는 자동 삭제를 두지 않는다

## 디자인 기준

`spec/design.md`의 나이트 테마는 **앱 화면** 기준이라 덱에는 쓰지 않는다. 발표는 9/20 13시,
조명이 켜진 강의실이다. 어두운 배경은 프로젝터에서 뭉갠다.

- 배경 60% 흰색, 본문 30% `#1F1F24`, 강조 10% 앰버 `#B45309`. 6:3:1
- 색은 세 가지를 넘지 않는다. 6장 대조에서만 상태 3색을 쓴다. 거기서는 색이 데이터다
- **16:9 고정.** 1280×720 프레임 하나를 화면에 맞춰 축소하고 바깥은 검게 칠한다.
  16:10 노트북에서는 위아래에 레터박스가 생긴다. PowerPoint와 같은 동작이다
- **제목은 항상 같은 자리.** 위에서 72px. 내용만 남은 공간에서 가운데 정렬한다
- 제품 화면을 글로 옮겨 적지 않는다. 1·4·6·8·10장은 `web/`을 실제로 띄워 찍은 캡처를 쓴다
- **캡처는 현재 React 층이다. BLE 발견 화면은 아직 없다.** 4장에 그렇게 적어두었다

### 문체 — 개조식, 단 조사는 살린다

명사로 끝내되(`~음`, `~점`, `~앱`) **조사와 연결어미까지 지우지는 않는다.**
조사를 전부 빼면 전보문이 된다. 한 번 그렇게 만들었다가 되돌렸다.

| 지나침 | 적당함 |
|---|---|
| `행사장 참가자 간 즉시 대화 연결` | `행사장에서 닿는 거리의 사람과 연결되는 앱` |
| `원인: 용기 부족이 아닌 대화 상대 불명확` | `용기가 부족해서가 아니라, 누구에게 말을 걸어야 할지 모르는 점이 원인` |
| `네트워킹 효과성 긍정 응답 주최자 비율` | `자기 행사의 네트워킹이 효과적이라고 답한 주최자 비율` |
| `GPS 미사용. 근접 판정 단말 내 처리` | `GPS 미사용. 근접 판정을 폰 안에서 끝내므로` |

기준: **소리 내어 읽어서 말이 되면 통과.** 명사만 이어붙여 읽히지 않으면 조사를 되살린다.

- 문장 끝은 명사나 `~음/~함/~점`. `~습니다`로 끝내지 않는다
- 짧은 라벨(`서버 미경유`, `구현 현황`, `핵심 지표`)은 조사 없이 둬도 된다
- 긴 설명은 조사를 살린다. 두 개 이상 명사를 이어붙이지 않는다
- **`A가 아니라 B` 대구를 쓰지 않는다.** 장마다 강조색 경구로 끝내지 않는다
- 줄표(—)를 쓰지 않는다. 가운뎃점(·)은 짧은 병렬(`조직·직무`)에만
- 백업 장의 질문(`qa-q`)만 예외. 심사위원이 실제로 말하는 문장이라 존댓말 그대로
- 화면은 개조식, 입으로 하는 말은 존댓말 대본(`spec/scenario.md`). 매체가 달라서 안 부딪힌다

**음슴체와 헷갈리지 말 것.** `~함/~음` 종결은 같지만 음슴체는 반말이고 게임 갤러리·카톡·
군대 무전에서 쓰는 축약체다. 나무위키가 "투박하다, 기계 같은 인상"이라고 정리한 그 어감이다.
개조식은 조사만 줄이고 격식은 유지한다.
[국립국어원 온라인가나다](https://www.korean.go.kr/front/onlineQna/onlineQnaView.do?mn_id=216&qna_seq=320204)

## 화면 캡처 다시 찍기

`assets/`의 다섯 장은 `web/`을 실제로 띄워 찍은 것이다. 앱이 바뀌면 다시 찍어야 한다.

1. `npm --prefix web run dev`
2. 뷰포트 **375×844**. 스크롤바는 CSS로 감춘다
   (`*::-webkit-scrollbar{width:0}` · `scrollbar-width:none`). 안 감추면 오른쪽에 줄이 남는다
3. 시연 인물로 입장한다. 이 인물이 덱의 숫자를 정한다

   | 칸 | 값 |
   |---|---|
   | 닉네임 | 지원 |
   | 자기소개 | 퇴근 후 첫 개인 앱을 만들고 있는데 배포에서 막혔어요. 발표 준비도 해야 해요 |
   | 찾는 사람 | 배포나 발표 해보신 분과 편하게 얘기하고 싶어요 |

   이 입력으로 **45명 중 5명**(민서·하윤·나연·채원·윤서)이 추천된다. 덱 5·8장의 숫자가 여기서 나온다.
4. `w1-entry`(입력만 채우고 제출 전) · `w2-room` · `w3-detail`(민서) · `w4-chat`
5. `w-detail-top`은 6장 히어로다. `html{zoom:3}`을 걸고 뷰포트를 **1125×2532**로 키워
   3배 해상도로 찍은 뒤 `(0, 345)`에서 **1122×1155**로 자른다. 확대가 아니라 실제 3배 렌더다

채팅은 **수신 메시지를 지어내지 않는다.** 두 사람으로 실제로 주고받아야 한다.
`bside:demo:v2:session`에 다른 참가자 id를 물린 세션을 넣고 새로고침하면 그 사람으로 답장할 수 있다.
저장된 값은 JSON 문자열이므로 `JSON.stringify(uuid)`로 넣는다.

## 남은 일

- [ ] **1장의 `TEAM ○`를 팀명으로 교체**
- [ ] **5장에 시연 영상 임베드** — PPTX에서 5번 장에 영상 파일을 얹는다. 링크가 아니라 파일로
- [ ] 5장 타임코드를 실제 촬영본에 맞게 수정
- [ ] 8장의 구현 목록을 실제 상태에 맞게 수정
- [ ] 리허설 5회

## 다시 뽑기

`deck.html`을 고친 뒤:

```bash
cd pitch && bash build.sh
```

본편과 백업이 각각 PDF·PPTX로 한 번에 나온다. 크롬과 `python-pptx`가 필요하다.

갈라내는 기준은 `deck.html`의 `data-backup` 속성이다. 백업으로 보낼 장에 그 속성을 붙이면 된다.

빌드 전에 `check.py`가 태그 균형을 본다. 닫는 태그가 하나 어긋나면 요소가 슬라이드 밖으로
새어나가 `#stage`에 붙고, **모든 장에 겹쳐 찍힌다.** 화면에서는 작은 얼룩으로만 보여 놓치기 쉽다.
